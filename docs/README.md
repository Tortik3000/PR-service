
Вопросы:
- нужно ли в getReviewer возвращать ошибку при отсутствии юзера(наверное нет)
    

Проблемы:
- id это строки

todo:
- dbmodel in repo???


metrics: 
- [x] График, показывающий RPS. Должна быть возможность посмотреть график по конкретному эндпоинту
- [x] График, показывающий latency. Должна быть возможность посмотреть график по конкретному эндпоинту
- [x] Количество успешных запрсов по каждому энпоинту 

- График, показывающий количество записей по каждой таблице в базе
- График, показывающий rate вставки записей по каждой таблице
- Графики для latency основных операций

- [x] График, показывающий количество горутин go_goroutines
- [x] График, показывающий live heap go_memstats_heap_inuse_bytes


Утверждения:
- в getReviewer не нужна транзакция так как проблемы:

    у user будет новый pr(ну и ладно)
  
    у pr обновится статус(ну и ладно)

- в setActive не нужна транзакция так как это атомарная операция

- teamGet не нужна транзакци т.r при обновлении ничего критичного не будет

- teamAdd нужна транзакци т.к возможно будет команда без пользователей

